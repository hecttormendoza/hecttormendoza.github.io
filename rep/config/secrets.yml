# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

shared:
  sendgrid_username: <%= ENV['SENDGRID_USERNAME'] %>
  sendgrid_password: <%= ENV['SENDGRID_PASSWORD'] %>
# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 7bc162e16e1d2a0d55a04fd3751cf10f67b658160908062c9435a36936a0f0808b02ce042c0da84fc2ca88513ee96ddd968a1dd868df18e6a13fdb68b95cb4e2

test:
  secret_key_base: 894a7471ef641f302695e284c879bceae976741ccb6440c844a1e32ac10176c9274aaaa9a4b9817d1fe8a9e8b55e26257b8e7e77dab2f8a68dafe62cc38f3ef5

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
